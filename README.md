# 🔐 Python Folder Locker (GUI)

A Windows desktop application built with Python (Tkinter) to lock folders
with per-folder passwords, attempt limits, and timed lockouts.

## ✨ Features
- Lock multiple folders
- Unique password per folder
- Attempt limit + wait time
- Encrypted storage (AES via Fernet)
- GUI-based (no command line)

## ⚠️ Security Note
This app is designed for personal privacy and workflow protection.
It does NOT protect against Windows administrators.

## 🛠 Requirements
- Python 3.10+
- Windows OS

## ▶️ Run
```bash
python gui.py
```
## IMPROVEMENT
- what will happen if the folder path is chnage during lock
- user must be able to unlock by not opening app, by just clicking the particular locked folder
- password change

